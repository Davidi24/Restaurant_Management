spring:
  profiles:
    active: native
  application:
    name: server-config
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configuration
server:
  port: 8888
